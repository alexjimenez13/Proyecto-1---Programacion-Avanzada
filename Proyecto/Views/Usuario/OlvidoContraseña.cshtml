@{
    Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
    ViewBag.Title = "Olvidé contraseña";
}

<form method="post" action="@Url.Action("OlvidoContraseña", "Usuario")">
    @Html.AntiForgeryToken()
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-header bg-dark text-white text-center">
                        <h4>Recuperar contraseña</h4>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label for="CORREO" class="form-label">Correo electrónico</label>
                            <input type="email" class="form-control" id="CORREO" name="CORREO" placeholder="Ingresa tu correo electrónico" required />
                        </div>
                        <button type="submit" class="btn btn-dark w-100">Recuperar</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@if (ViewBag.Mensaje != null)
{
    <p style="color:green">@ViewBag.Mensaje</p>
}

@Html.ValidationSummary(true)


@*@{
        Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
        ViewBag.Title = "Olvidé contraseña";
    }

    <form method="post" action="@Url.Action("OlvidoContraseña", "Usuario")">
        @Html.AntiForgeryToken()
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="card shadow">
                        <div class="card-header bg-dark text-white text-center">
                            <h4>Recuperar contraseña</h4>
                        </div>
                        <div class="card-body">
                            <div class="mb-3">
                                <label for="correoElectronico" class="form-label">Correo electrónico</label>
                                <input type="email" class="form-control" id="correoElectronico" name="correoElectronico" placeholder="Ingresa tu correo electrónico" required />
                            </div>
                            <button type="submit" class="btn btn-dark w-100">Recuperar</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>

    @if (ViewBag.Mensaje != null)
    {
        <p style="color:green">@ViewBag.Mensaje</p>
    }

    @Html.ValidationSummary(true)*@


@*@{
        Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
        ViewBag.Title = "Olvidé contraseña";
    }

    <form method="post" action="@Url.Action("OlvidoContraseña", "Usuario")">
        @Html.AntiForgeryToken()
        <div id="react-forgot-password"></div>
    </form>

    @if (ViewBag.Mensaje != null)
    {
        <p style="color:green">@ViewBag.Mensaje</p>
    }

    @Html.ValidationSummary(true)

    @section Scripts {
        <script type="text/babel">
            function ForgotPasswordForm() {
              return (
                <div className="container mt-5">
                  <div className="row justify-content-center">
                    <div className="col-md-6">
                      <div className="card shadow">
                        <div className="card-header bg-dark text-white text-center">
                          <h4>Recuperar contraseña</h4>
                        </div>
                        <div className="card-body">
                          <div className="mb-3">
                            <label htmlFor="correoElectronico" className="form-label">
                              Correo electrónico
                            </label>
                            <input
                              type="email"
                              className="form-control"
                              id="correoElectronico"
                              name="correoElectronico"
                              placeholder="Ingresa tu correo electrónico"
                              required
                            />
                          </div>
                          <button type="submit" className="btn btn-dark w-100">
                            Recuperar
                          </button>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              );
            }

            // Monta el componente en el contenedor "react-forgot-password"
            ReactDOM.render(<ForgotPasswordForm />, document.getElementById("react-forgot-password"));
        </script>
    }*@
