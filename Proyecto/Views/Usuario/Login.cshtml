@model Proyecto.Models.Usuario

@{
    Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
    ViewBag.Title = "Login";
}

<form method="post" action="@Url.Action("Login", "Usuario")">
    @Html.AntiForgeryToken()
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-header bg-dark text-white text-center">
                        <h4>Inicio de sesión</h4>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label for="NOMBRE_USUARIO" class="form-label">Nombre de Usuario</label>
                            <input type="text" class="form-control" id="NOMBRE_USUARIO" name="NOMBRE_USUARIO" placeholder="Ingresa tu usuario" required />
                        </div>
                        <div class="mb-3">
                            <label for="CONTRASENA" class="form-label">Contraseña</label>
                            <input type="password" class="form-control" id="CONTRASENA" name="CONTRASENA" placeholder="Ingresa tu contraseña" required />
                        </div>
                        <button type="submit" class="btn btn-dark w-100">Ingresar</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@Html.ValidationSummary(true)

<p class="text-center mt-4">
    ¿Olvidaste tu contraseña?
    @Html.ActionLink("Recuperar aquí", "OlvidoContraseña", "Usuario")
</p>

<p class="text-center mt-4">
    Registrate aquí:
    @Html.ActionLink("Registrarse", "Registro", "Usuario")
</p>



@*@model Proyecto.Models.Usuario

    @{
        Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
        ViewBag.Title = "Login";
    }

    <form method="post" action="@Url.Action("Login", "Usuario")">
        @Html.AntiForgeryToken()
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="card shadow">
                        <div class="card-header bg-dark text-white text-center">
                            <h4>Inicio de sesión</h4>
                        </div>
                        <div class="card-body">
                            <div class="mb-3">
                                <label for="nombreUsuario" class="form-label">Nombre de Usuario</label>
                                <input type="text" class="form-control" id="nombreUsuario" name="nombreUsuario" placeholder="Ingresa tu usuario" required />
                            </div>
                            <div class="mb-3">
                                <label for="contraseña" class="form-label">Contraseña</label>
                                <input type="password" class="form-control" id="contraseña" name="contraseña" placeholder="Ingresa tu contraseña" required />
                            </div>
                            <button type="submit" class="btn btn-dark w-100">Ingresar</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>

    @Html.ValidationSummary(true)

    <p class="text-center mt-4">
        ¿Olvidaste tu contraseña?
        @Html.ActionLink("Recuperar aquí", "OlvidoContraseña", "Usuario")
    </p>

    <p class="text-center mt-4">
        Registrate aqui:
        @Html.ActionLink("Registrarse", "Registro", "Usuario")
    </p>*@











@*@model Proyecto.Models.Usuario

    @{
        Layout = "~/Views/Shared/_LayoutIngreso.cshtml";
        ViewBag.Title = "Login";
    }

    <form method="post" action="@Url.Action("Login", "Usuario")">
        @Html.AntiForgeryToken()
        <div id="react-fields"></div>
    </form>

    @Html.ValidationSummary(true)

    <p class="text-center mt-4">
        ¿Olvidaste tu contraseña?
        @Html.ActionLink("Recuperar aquí", "OlvidoContraseña", "Usuario")
    </p>

    @section Scripts {
        <script type="text/babel">
            function LoginFields() {
              return (
                <div className="container mt-5">
                  <div className="row justify-content-center">
                    <div className="col-md-6">
                      <div className="card shadow">
                        <div className="card-header bg-dark text-white text-center">
                          <h4>Inicio de sesión</h4>
                        </div>

                        <div className="card-body">
                          <div className="mb-3">
                            <label htmlFor="nombreUsuario" className="form-label"> Nombre de Usuario </label>
                            <input type="text" className="form-control" id="nombreUsuario" name="nombreUsuario" placeholder="Ingresa tu usuario" required/>
                          </div>

                          <div className="mb-3">
                            <label htmlFor="contraseña" className="form-label">
                              Contraseña
                            </label>
                            <input type="password" className="form-control" id="contraseña" name="contraseña" placeholder="Ingresa tu contraseña" required
                            />
                          </div>

                          <button type="submit" className="btn btn-dark w-100">
                            Ingresar
                          </button>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              );
            }

            ReactDOM.render(<LoginFields />, document.getElementById("react-fields"));
        </script>
    }*@
